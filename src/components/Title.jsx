import Logo from "../pictures/Logo.png";
const Title = (props) => {
  return (
    <>
      <div className="section">
        <header>
          <img src={Logo} />
        </header>
      </div>
    </>
  );
};

export default Title;
